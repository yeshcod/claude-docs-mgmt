# Changelog — {{PROJECT_NAME}}

Append-only log of removed features, completed migrations, finished renames. Anything that would otherwise leave a "what happened to X?" gap in the docs.

Newest on top.

## {{CURRENT_DATE}} — Documentation framework initialized

- Bootstrapped via `claude-docs-mgmt` `/init-docs` skill (fullstack-web profile).
- Created: `CLAUDE.md`, `BUGS.md`, `TEST_CASES.md`, `ROADMAP.md`, `.claude/docs/*`.

<!-- Append new entries above this line. Template:

## YYYY-MM-DD — <short title>

- <what happened> (<PR or commit SHA>).
- <why>.
- <what replaced it, if anything>.

-->
